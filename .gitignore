# macOS / iOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Working Copy / iCloud cruft
*.icloud

# Editors
.vscode/
*.swp
*~

# ── Legacy / archived directories (no longer part of the active loopy-mapper-react project) ──
# These are kept on disk for reference but excluded from the repo.
# Anchored to the repo root so they don't shadow same-named dirs inside
# loopy-mapper-react/ (e.g. loopy-mapper-react/scripts/).
/ipad/
/Loopy Project/
/projects/
/docs/
/drambo/
/scripts/
/tools/
*.lpproj
CLAUDE.md

# Old vanilla JS web app (except the example patches that are still referenced)
web/loopy-mapper/
!web/loopy-mapper/examples/

# Downloaded reference material (third-party, kept local — not committed)
userdl/

# ── Build artifacts ──
__pycache__/
*.pyc
*.pyo
dist/
dist-ssr/
*.local

# ── Python virtual environment ──
.venv/
venv/
env/

# ── MIDI datasets (too large, run the ingest scanner locally) ──
datasets/

# ── SQLite database (regenerated by the ingest scanner) ──
midi_library.db

# ── Claude Code per-machine settings (local, not shared) ──
.claude/settings.local.json

# Backend drums directory (per-machine, set via app Settings)
loopy-mapper-react/backend/drums_config.json
